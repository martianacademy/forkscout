/**
 * Knowledge consolidator barrel — re-exports the Consolidator class and types.
 *
 * @module memory/consolidator
 */

export { Consolidator } from './consolidator';
export type { ConsolidationConfig, ConsolidationResult } from './types';
