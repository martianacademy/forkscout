const fs = require('fs');\nconst path = require('path');\n\n// Load archetypes\nconst archetypesPath = path.join(__dirname, 'archetypes.json');\nconst archetypes = JSON.parse(fs.readFileSync(archetypesPath, 'utf8'));\n\n// Gen 3 random missions\nfunction generate(seed = Date.now()) {\n  const shuffled = archetypes.sort(() => Math.random() - 0.5).slice(0, 20); // Sample 20\n  const missions = [];\n  for (let i = 0; i < 3; i++) {\n    const arch = shuffled[Math.floor(Math.random() * shuffled.length)];\n    missions.push({\n      id: `${seed}_${i}`,\n      desc: arch.desc,\n      params: { seed: `${seed}_${i}`, rand_str: Math.random().toString(36).slice(2) },\n      expected: `Solved via ${arch.domains.join(',')}`,\n      domains: arch.domains\n    });\n  }\n  const output = { seed, missions };\n  fs.writeFileSync(path.join(__dirname, `bench_${seed}.json`), JSON.stringify(output, null, 2));\n  console.log(`Generated bench_${seed}.json:`, output);\n  return output;\n}\n\nif (require.main === module) {\n  generate();\n}