# Forkscout Secrets — API keys & tokens only
#
# All non-secret settings (models, tiers, budget, agent config) live in
# forkscout.config.json at the project root.
#
# Copy this file to .env and fill in your keys.

# ── Provider API Keys ────────────────────────────────────
# Add your API keys here. Only fill in the providers you use.
# The router auto-picks the right key based on each tier's provider.
OPENROUTER_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# ── Admin & Integrations ────────────────────────────────
ADMIN_SECRET=

# Telegram Bot (optional — set to enable Telegram bridge)
TELEGRAM_BOT_TOKEN=
