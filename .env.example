# Forkscout Secrets — API keys & tokens only
# All other settings live in forkscout.config.json
#
# Copy this file to .env and fill in your keys.

# ── Provider API Keys & URLs ─────────────────────────────
# Only fill in the providers you use. URLs have sensible defaults — override only if needed.
OPENROUTER_API_KEY=
# OPENROUTER_API_URL=https://openrouter.ai/api/v1

OPENAI_API_KEY=
# OPENAI_API_URL=https://api.openai.com/v1

ANTHROPIC_API_KEY=
# ANTHROPIC_API_URL=https://api.anthropic.com/v1

GOOGLE_API_KEY=
# GOOGLE_API_URL=https://generativelanguage.googleapis.com/v1beta

OPEN_API_COMPATIBLE_API_KEY=
# OPEN_API_COMPATIBLE_API_URL=

# ── Admin & Integrations ────────────────────────────────
ADMIN_SECRET=

# Telegram Bot (optional — set to enable Telegram bridge)
TELEGRAM_BOT_TOKEN=

# ── Deployment Overrides ────────────────────────────────
# SearXNG URL (varies per environment — Docker: http://searxng:8888)
# SEARXNG_URL=http://localhost:8888#
# Agent port (default: 3210)
# AGENT_PORT=3210
